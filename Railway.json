{
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "backend/Dockerfile",
    "rootDirectory": "backend"
  },
  "deploy": {
    "startCommand": "./server",
    "restartPolicyType": "ON_FAILURE"
  }
}
